# ZURE — 次回作業メモ（未実装 / hayato さんからの指示 2026-08-15）

ユーザーの指示により記録。**まだ実装しないこと。** 次のセッションで
このファイルを読んでから着手する。

## 1. パラメータに ATTACK を追加

VOICE EDIT の各行（KICK / SNR / CH / OH / TONE）に ATTACK ノブを足す。
現在のエンベロープは AD(S)（アタックは声部ごとの固定値）。
`ampEnvADS()` の `atk` 引数を `vp` から引くだけの構造にはなっている。
デフォルトは現行の固定値（キック 1〜3ms、トーン 8〜14ms 等）を使い、
音を変えないこと。テスト（test_voices.js）の P() ヘルパにも atk を足す。

## 2. SUSTAIN の挙動変更

原文:「サスティンはあくまでクリックシタぶんの長さだけでなるようにする」

解釈: 現在は「次の同じ行のヒットまで持続（モノボイス・チョーク）」だが、
そうではなく **押した（クリックした／ゲートの）長さのぶんだけ** 鳴らす。
おそらくゲートタイム方式 — ステップ入力の長押し時間、またはステップ長
（＝1ステップ分のゲート）を基準にする。着手前に本人へ確認するのが安全:
- 案A: ゲート＝1ステップ長（テンポ追従）で、SUS はそのゲート中のレベル
- 案B: パッド/セルを押している実時間だけ持続（演奏操作）
現行の「次ヒットまで持続」は廃止または選択式に。8秒キャップと
STOP 時解放は維持。

## 3. PHASE SCOPE をアナログブラウン管（CRT）風に

原文:「アナログのブラウン管の要素をいれる。フェアライトの感じを想像」

Fairlight CMI のモニタの佇まい:
- 緑色蛍光体（グリーンフォスファ）のベクター表示、黒地
- 残光（フォスファの減衰トレイル）— ヘッドの軌跡がじわっと消える
- わずかなブルーム/グロー、走査線・ビネットは控えめに
- 現在の 92×92 pixelated 描画は維持しつつ、パレットと残光を変える
（92年PCモノトーンの現デザインの中で、スコープだけ CRT なのは
Fairlight 的で正しい）

## 4. クレジット表記

デザイン（UI 上の表記）に **SCIRE, hayato YAMADA** と入れる。
場所の候補: ヘッダーのロゴ下（POLYTEMPO LOOP MACHINE の並び）、
または ABOUT パネルのフッタ。<title> はそのまま。

---
状況メモ: 現在 zure.html は 92年PC風ピクセルスキン（1bit ディザ）。
PR は scire-h/scire-h#16（draft、CI green、head 130eda7 時点）。
テストは tests/web/ の3スイート 296 アサーション + scratchpad の
Playwright E2E 群。suites は zure.html からマーカーでコードを切り出す
方式なので、実装時は VOICES / TEMPO_CORE / RECORDER_CORE マーカーを
壊さないこと。
